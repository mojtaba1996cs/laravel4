kind: wasmer.io/App.v0
name: laravel4
package: .
capabilities:
  database:
    engine: mysql
locality:
  regions:
    - fr-roub1 
env:
  APP_ENV: production
  APP_DEBUG: "true" 
  APP_KEY: "base64:STyEX+Yi0BF3oKVufCv0nkum7oo2FJydWDF1fohdwBY=" 
  DB_CONNECTION: mysql
jobs:
  - name: run-migrations
    trigger: post-deployment
    action:
      execute:
        command: artisan
        cli_args:
          - "migrate"
          - "--force"
